{
  "name": "pulse-action-blog",
  "version": "1.0.0",
  "description": "Full-Stack Actionable Blog Platform with Interactive Action Checklists, Polls, and Audio Narration",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm run --prefix server dev",
    "client": "npm run --prefix client dev",
    "build": "npm run --prefix client build",
    "start": "npm run --prefix server start",
    "install-all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
